# python.study
```shell
# export
$ pip freeze > requirements.txt

# install
$ pip install -r requirements.txt

# uninstall
$ pip uninstall -r requirements.txt -y

# execute
$ python3 SearchRankService.py >> app.log 2>&1 &
$ python3 AfterHours.py >> app_after_hours.log 2>&1 &
```
